# self-evolving-embodied-intelligence-architectural-blueprint

> **文档地位**：本文保留为研究背景与候选机制目录，不再作为首版实施顺序或稳定接口的规范。当前实现以已接受的 ADR 和已关闭的 Wayfinder 决议为准；发生冲突时，优先采用 [ADR-0001](docs/adr/0001-freeze-only-the-physical-research-loop-contracts.md) 与 [ADR-0002](docs/adr/0002-reach-first-success-through-one-vertical-slice.md)。本文中关于设备盖状态、VLA-only、人工预采集评估数据或预建多层平台的旧描述均已被后续决议替代。

> **对齐框架**：宇树"Unitree 自进化循环"（感知 → 决策 → 执行 → 评估 → 学习 → 进化），节点为二级标题
> **目标平台**：宇树 G1 人形机器人（手臂 + BrainCo 灵巧手，四路相机）
> **首个目标技能**：点击黄色按钮（当前 0 成功率）
>
> **v0.3 相对 v0.2 的三项结构性变更**：
> 1. **零标注界面 + 视觉优先判定**：人的参与仅剩物理复位、初始安全包络定义与（可选）安装签发。成功判定默认由 VLM 看图完成（"盖子开了吗"）；诊断态操作员在场复位时顺带目击确认。模板差分/轻量分类器**不进主线**，仅在 C3 残差 RL 启动时随需构建。
> 2. **策略后端解绑 ACT**：不再默认任何神经网络为主角。候选按"每样本可解释性"排序，程序化候选优先；ACT 降级为候选类别之一 + 诊断基线。UniFoLM-VLA 为跨技能阶段的挑战者；UniFoLM-WMA **降为旁路件**，不进 0→1 主线。
> 3. **吸收外部蓝图精华**：对另一份独立蓝图做了第一性原理对比（见第 0.5 章），采纳其候选中心主义、可证伪假设纪律、三平面权限、八类失败路由、发现/可安装两级里程碑与进化度量；拒绝其人工纠正依赖、全局禁止在线更新与旧仓库锚定。

---

## 0. 第一性原理（修订版）

### 0.1 公理与四个不可约简组件

公理：**学习 = 用行为的真实后果改进行为**。系统必须且只须包含：

1. **真值发生器（评估）**——把物理世界的后果翻译成机器可读信号；
2. **行为发生器（执行）**——安全、廉价、大批量地产生有信息量的尝试；
3. **改进机制（学习 + 进化）**——参数通道（改权重）与配置/实现通道（改课程、约束、代码、候选）正交并存；
4. **资产化机制（沉淀）**——技能一旦达成，永远可调用、可回归、可回滚。

### 0.2 三种必须分离的事实

任何回合结束时存在三个独立命题，混淆它们是自欺的根源：

```
执行完成（进程正常返回、命令按时发完）
≠ 命令被跟踪（关节反馈与命令一致）
≠ 任务成功（独立 Oracle 确认仪器盖 closed→open）
```

只有第三项能向学习循环提供正奖励。系统的一切记录与判定围绕这三层分离设计。

### 0.3 约束推导的六条设计定理

- **T1 · 探索必须被先验压缩**（无遥操作 ⇒ 无人在线纠正）：先验来源 = 基础模型视觉常识 + 标定几何 + 已有少量数据 + 现有 checkpoint。
- **T2 · 评估优先用视觉，复杂度与对抗压力成比例**：黄色按钮的成功判据（盖子开没开）在视觉上显而易见。主线是程序候选（C1）时，没有优化器会去骗判定器——VLM 看图 + 诊断态操作员目击确认已经足够。模板差分、开合校准仪式、轻量分类器是**对抗 RL 的防御**，不是判定本身；只在 C3 启动时随需构建。设备无电气信号、无标记物 ⇒ 不发明硬件真值通道。
- **T3 · 人力只花在物理复位**：人 = 场务（合盖，秒级）+ 一次性安全包络定义 +（可选）安装签发。诊断态的目击确认是复位的免费副产品，不是标注界面。
- **T4 · 后端选型以"闭环增益 × 可解释性 ÷ 样本需求"为准绳**：固定单场景的 0→1 首选**失败可归因**的实现——程序化候选每次失败都能定位到"感知/规划/执行/接触"某一级；端到端网络失败时只能得到一个模糊的"策略错了"。可解释性不是审美偏好，而是**失败路由（第⑥节点）能否运转的前提**。
- **T5 · 物理风险由动作空间界定，而非权重是否冻结**：在硬约束沙箱（工作空间盒 + 限速 + 有界残差）内，在线更新权重的策略与任何冻结策略的最坏物理行为相同；因此**沙箱内允许在线学习，沙箱外或申请扩权的候选必须冻结 digest 走门控晋级**。
- **T6 · 记录做加法，机制做减法**（本修订新增）：回合记录错过即永失，必须全量；机制复杂度必须与当前对抗压力和自主程度成比例。为省一次 1 分钟真机回合而去微调世界模型、为二元视觉事实去搭三通道判定栈，都是用工程复杂度取代视觉。需要时再建，不提前付费。

### 0.4 关键结构转变：学习的主角色从"发现"变为"巩固"

v0.2 隐含假设"0→1 由 RL 完成"。第一性原理重新审视：固定场景的按钮按压，其感知（黄色按钮在哪）、规划（怎么接近）、大部分执行（移动到位）都在基础模型 + 标定几何的能力范围内；真正不确定的只有**最后几毫米的接触动力学**。因此：

```
0→1 的主攻手：程序化候选（VLM 关键点 → 3D 定位 → 受限 IK → 参数化按压原语）
RL 的岗位：仅当失败被归因为 contact_dynamics 时，在沙箱内训练有界残差
学习的主角色：巩固（consolidation）——把程序候选积累的成功轨迹
              蒸馏成反应式闭环策略，获得速度与抗扰动，而非发现行为
```

这正是 HERO（零人工示教，2026）验证的三级结构：L1 启发推理 → L2 范例复用 → L3 反应式巩固。若程序候选直接成功，它就是真实的 0→1——**技能没有义务是神经网络**（采纳外部蓝图立场）。

---

## 0.5 两份蓝图的第一性原理对比

对另一位 agent 的蓝图（Architecture Blueprint v0.1 + 前沿调研文档）逐项审计。其承重引用（ENPIRE、HERO、Q-Planning、VERITAS、AutoSERL、UniFoLM 等）已独立核实为真。

### 采纳的精华（附理由）

| 采纳项 | 内容 | 采纳理由 |
|---|---|---|
| **候选中心主义** | 系统迭代对象是"任务的可执行实现"（program / policy / hybrid），不是 checkpoint 版本号 | 与 T4 一致；解除"必须是端到端网络"的隐含枷锁，程序候选成为一等公民 |
| **可证伪假设纪律** | 每个候选必须绑定"这次改了什么、预期修复哪种已观察失败"；禁止"改一堆再试试" | 可解释性的最小单位；没有它，成功了也不知道为什么，无法沉淀 |
| **三平面权限模型** | 获取面（可自进化）/ 执行面（不可自我改写）/ 持久面（append-only）；唯一真机入口，写权限短时、显式、可撤销 | 比 v0.2 的"验证门"更完整的权限代数；LLM/训练器/仿真器一律无电机权 |
| **八类失败路由** | perception / planning / policy_action / adapter_or_limiter / control_tracking / contact_dynamics / oracle_or_recording / safety 八类，各配证据与下一动作 | 比 v0.2 的五类账本更精细；尤其 adapter/limiter 与 oracle/recording 两类是工程实践中最常被误诊为"模型不行"的 |
| **实验选择器** | 便宜证据优先：已有数据诊断 → 离线回放 → 影子运行 → 真机回合；utility = 信息增益 + 成功概率 − 成本 − 风险 | 把"少样本"正确地重构为实验选择问题而非算法问题 |
| **发现 ≠ 可安装** | 首次成功只是 discovery；冻结候选后从 fresh reset 独立复现才可安装；安装含最小实现修剪、回归测试、revision 与 rollback | 防止"调参调出来的一次成功"被误当技能 |
| **pass@k 诚实记账** | 重试后成功、恢复后成功、完全自主一次成功分开统计 | 否则学习器会优化错误目标 |
| **进化度量** | rollouts-to-first-success、diagnosis_resolution_rate、candidate_reuse_rate、第二技能成本下降等 | "系统在进化"的可证伪定义，取代"模型版本数"虚荣指标 |
| **abstain 语义** | 前置条件不满足或置信不足时技能必须拒绝执行，而非试探 | 契约完整性 |
| **新文献** | ENPIRE（总循环范式）、HERO（零示教三级结构）、Q-Planning（冻结 actor + 独立 Q 从失败学习）、VERITAS（生成-验证数据飞轮）、UniFoLM（G1 原生 VLA/世界模型） | 全部核实存在；分别填入本蓝图对应岗位 |

### 拒绝的糟粕（附理由）

| 拒绝项 | 对方立场 | 拒绝理由 |
|---|---|---|
| **人工纠正作为核心路径** | InterventionLearner、HG-DAgger 式接管、"请求一次 correction"贯穿其 Round 3 与 learner portfolio | 双重违反本项目硬约束：无遥操作设备（correction 需要接管硬件）且零人工标注（correction 本质是最昂贵的标注）。仅保留为文献参考 |
| **全局禁止在线权重更新** | "真机 actor 始终是冻结 digest"，一切更新走批式晋级 | 违反 T5：安全由动作空间硬约束保证，与权重更新时序无关。全冻结使沙箱内残差 RL 退化为低效批式迭代，牺牲样本效率换取一个已由安全链提供的保证。修正为**两级执行权限**（见 5.2） |
| **密集人工授权门** | campaign 批准、高风险变更审批、具名硬件批准、安装批准层层设人 | 与自进化目标冲突且违反零标注精神。收缩为：初始安全包络一次性定义 + 可选的安装签发；批次级决策全自动 |
| **旧仓库锚定** | 大量篇幅映射其既有代码（vegapunk 模块、v14 trace 路径） | 正是用户警示的一致性偏误。本蓝图只吸收机制，不继承任何旧资产结构 |
| **单回合节奏一刀切** | 每轮"一条受监督真机回合" | 只适合程序候选的诊断态；学习候选需要批量自主回合。修正为**双态执行制度**（见 5.3） |
| **人工 success bit 兜底** | 其引用的 Q-Planning 真机奖励是每回合人工标注的成功位 | 不作为训练态奖励。诊断态操作员本就在场复位，目击确认是免费审计，不是标注界面；主线判定用 VLM 看图 |

### 0.6 过度设计审计（本修订的第一性原理结论）

用户指出 Oracle 小题大做。该批评正确，且暴露了更普遍的错误：**把"将来可能需要"的防御当成"现在必须建造"的基础设施。**

| 机制 | 判定 | 理由 |
|---|---|---|
| Oracle 三通道（模板差分 + 校准仪式 + 分类器） | **主线删除** | 对抗压力只来自 RL；C1 程序候选不会骗判定器。"盖子开没开"视觉显而易见，VLM 看图即可。防骗加固随 C3 启动再建 |
| 诊断态人工目击 | **保留为免费审计** | 操作员本来就在场复位；按一个键或口头确认，不是标注界面，也不是训练信号 |
| UniFoLM-WMA 影子预测 | **移出主线，降为旁路** | 为省一次约 1 分钟真机回合，先要微调世界模型 + 留出集验证 + 持续校准，工程成本远超收益。C1 诊断态回合极少，不需要学习化预筛 |
| 黑板的 Redis 事件总线 / 六存储服务化 | **首版降为文件目录 + git** | 单机单任务不需要服务拓扑；黑板是数据契约，不是基础设施。第二技能或训练态批量后再升级 |
| 七层安全链 | **保留** | 安全冗余由不可逆性正当化，不是过度设计 |
| 全量 EpisodeArtifact | **保留** | 记录做加法：错过即永失 |

---

## 循环总览

```
        ┌───────────────────────────────────────────────────────────┐
        │            ① 大模型驱动（循环中枢，无电机权）                  │
        │  Orchestrator 指挥 / Coder 候选工程师 / Analyst 归因 /        │
        │  Auditor 抽检判定分歧、审查脏成功）                            │
        └────────┬──────────────────────────────────▲───────────────┘
      工件: 候选(含假设)、配置diff、工单               │ 批次报告、经验笔记、审计结果
                 ▼                                  │
        ┌─────────────────────┐          ┌──────────┴────────────┐
        │ ④ 代码生成·数据处理    │          │ ⑥ 结果分析·经验总结     │
        │   模型训练·技能调用    │          │   上下文优化            │
        │ （候选工厂+零标注精炼   │          │ （八类失败路由+实验选择   │
        │   +按需训练+技能库）   │          │   +配置进化）           │
        └────────┬────────────┘          └──────────▲────────────┘
      部署: 冻结候选 / 沙箱内在线学习器                 │ 回合工件流
                 ▼                                  │
        ┌───────────────────────────────────────────┴─────────────┐
        │ ⑤ 仿真验证 → 真机部署 → 任务执行 → 状态反馈                  │
        │（MuJoCo 预检门 │ 两级执行权限 │ 双态执行 │ 全量记录）           │
        └─────────────────────────────────────────────────────────┘
   首版黑板 = 文件目录 + git；唯一真机入口在 ⑤ 的安全执行链
```

> 原图中 **② Unitree 定义技能** 与 **③ 自动调研** 两节点按用户指令跳过。

---

## ① 全球顶尖 AI 大模型驱动（循环中枢）

### 职责边界

大模型承担一切符号智能，产出永远是**工件**（候选、配置、报告、工单），永远不产出电机指令。工件必须通过三级验证门（静态检查 → 回放干跑 → 运动学检查）才能生效。这条边界与 ENPIRE 的实证一致：coding agent 分析日志、改进算法与基础设施，但真机执行走不可变的环境 API。

### 四角色（同一模型，不同提示与工具集）

| 角色 | 职责 | v0.3 变更 |
|---|---|---|
| **Orchestrator 指挥** | 读批次报告与经验库，运行实验选择器，决定下一批"最值得买的证据"；批准候选晋级；签发技能 | 决策依据从"配置调整"升级为"候选组合管理"（该派程序候选、残差 RL 还是巩固蒸馏） |
| **Coder 候选工程师** | 生成/修改**候选本身**：程序候选代码（关键点→IK→原语）、课程/奖励/约束配置、复位脚本 | 从"配置工程师"升格为"候选工程师"；每个候选必须附可证伪假设，否则验证门直接拒收 |
| **Analyst 归因分析师** | 按八类失败路由做分层归因；证据不足时输出 `unknown` 并设计鉴别实验，禁止编造确定原因 | 归因类别从五类扩为八类；新增"设计鉴别实验"职责 |
| **Auditor 审计员** | 抽检 VLM 判定与诊断态目击是否一致；审查依赖大量限幅的可疑成功 | 不再承担"系统内唯一标注者"；主线判定就是 VLM 看图 |

### 技术选型

前沿多模态大模型（API 或本地 Qwen-VL 级）；结构化工具调用 + JSON Schema；提示词、决策、工件全部 git 版本化；经验笔记与技能卡向量检索（Voyager 式）；大模型只看分层压缩摘要与关键帧，不看原始视频流（REFLECT 教训）。

---

## ④ 代码生成、数据处理、模型训练、技能调用

### 4.1 候选工厂（原"工件工厂"升格）

**候选契约**（采纳外部蓝图并精简）：

```json
{
  "candidate_id": "c-023",
  "kind": "program | residual_policy | consolidated_policy | vla_adapter | hybrid",
  "hypothesis": {
    "change": "按压原语的接触速度从 2cm/s 降至 0.8cm/s，增加 0.5s 保持",
    "expected_fix": "contact_dynamics: 触达但按压未使盖子弹开（批次06观察）"
  },
  "artifact_digest": "sha256:...",
  "parent_candidates": ["c-019"],
  "obs_contract": "...", "action_contract": "...",
  "required_permission": "sandbox | gated"
}
```

**候选组合**（按"每样本可解释性"排序，岗位制而非主角制）：

| 候选类别 | 实现 | 岗位 | 失败可归因性 | 样本需求 |
|---|---|---|---|---|
| **C1 程序候选** | GroundedSAM 定位黄色按钮 → 多视角/深度恢复 3D 关键点与法向 → 受限 IK 规划 接近→接触→按压→撤离 → 参数化按压原语（深度/速度/保持时长/手指预置） | **0→1 主攻手**（HERO L1） | 每级可单独归因 | 0 |
| **C2 范例复用** | 成功轨迹存为 exemplar，相似初始条件下空间变换后重放 | 快速积累成功数据（HERO L2） | 高 | 1 条成功 |
| **C3 有界残差 RL** | 冻结的 C1/C2 标称轨迹 + 沙箱内 RLPD 训练的 ±毫米级残差；奖励 = Oracle 稀疏信号 + 几何进度（自动计算，零标注） | 仅当失败 = contact_dynamics 时启用 | 中（残差幅度可视化） | 数十~百回合 |
| **C4 巩固策略** | C1–C3 积累的成功轨迹（自生成、零人工示教）蒸馏为闭环视觉运动策略（ACT / Diffusion Policy 头，此时才轮到它们上场） | 达成后的速度与抗扰动升级（HERO L3 / RLDG） | 低，但此时技能已达成，风险由回归测试兜底 | 数百条自生成 |
| **C5 UniFoLM-VLA 适配** | 宇树开源 G1 原生 VLA（Qwen2.5-VL-7B 底座）+ 小适配头 | 语义泛化挑战者；跨技能阶段启用 | 低 | 中 |
| **横切 · UniFoLM-WMA** | 宇树开源世界模型-动作框架 | **旁路件，不当 actor，不进 0→1 主线**。岗位：技能已达成后的影子预测 / 合成数据。门槛：留出集证明能排序成功/失败动作后才有投票权 | 预测误差可视 | 用自生成回合微调 |

**现有 ACT checkpoint 的去向**：降级为（a）诊断基线——对其做三动作流归因，回答"它到底败在哪一级"，这本身是高价值证据；（b）C4 巩固阶段的可选初始化。不再默认给它加版本号重训。

### 4.2 数据处理——零标注经验精炼

回合结束自动触发：

1. **同步与完整性检查**：四路相机、本体状态、逐 tick 动作血统（候选原始动作 → 适配器输出 → 安全链投影命令 → 关节实际反馈，四流对齐）；
2. **判定标注**：VLM 看独立相机终帧（"仪器盖是开还是关？"）→ `succeeded | failed | indeterminate`；诊断态操作员目击确认写入同一字段的 `human_witness` 旁注（不替代 VLM，作审计）。分歧记 indeterminate，该回合不得当正负样本。
3. **分段可用性**（采纳外部蓝图 segment-level eligibility，剔除其人工 correction 段）：自主成功段 → 巩固蒸馏正样本；自主失败前缀 → Q/critic 与失败分类训练；`indeterminate` 段 → 隔离，禁止混入任何训练；"脏成功"（高限幅/安全事件）→ 降权或剔除；
4. **范例库登记**：成功轨迹连同初始条件指纹存为 exemplar（C2 的原料）；
5. **血统登记**：每条样本挂候选 digest + 配置 digest + Oracle 版本。

### 4.3 模型训练——按需训练栈

**L0 · Oracle（视觉优先，默认一层）**
- **主线判定**：回合结束，把 Oracle 独立相机的终帧（及复位时的 closed 对照帧）交给 VLM："这张图里仪器盖是开还是关？" 输出 `open | closed | uncertain`。盖子开没开在视觉上显而易见——这就是全部机制。
- **诊断态目击**：操作员本就在场合盖复位；回合结束时口头或按一键确认，写入 `human_witness`。这是免费审计，不是标注界面，也不是训练态奖励。
- **不进主线的东西**：开合校准仪式、模板差分、ROI 阈值、轻量分类器。它们是**对抗 RL 的防御**，不是判定本身。
- **C3 触发时的升级（随需）**：残差 RL 启动前，用已积累的 VLM 判定帧（诊断态已有对照）训练一个低延迟分类器，作在线奖励；VLM 仍作终审抽检。此时才需要防骗：Oracle 相机与策略观测隔离、假阳率监控、分歧则暂停训练。届时回合库已有数据，训分类器是一条命令，不是 P0 阻塞项。

**L1 · 沙箱内在线学习器**（条件启用）
- C3 残差 RLPD：actor-learner 异步（agentlace/LeRobot 实现），**允许在线权重更新**——安全由沙箱结构保证（T5）；
- 课程进度信号 = 标定感知自动计算的几何量（指尖-按钮距离、压深）。

**L2 · 巩固与蒸馏**
- C4：自生成成功轨迹 → ACT/Diffusion 头蒸馏（HERO L3）；
- 跨技能阶段：RLDG 式蒸馏回 UniFoLM-VLA / 通用策略。

### 4.4 技能调用——技能包与执行服务

**技能包**（采纳外部蓝图的完整 bundle 结构）：

```text
press_yellow_button@1/
├── contract.yaml        # 目标、前置/后置条件、abort 条件、超时、安全预算
├── implementation.yaml  # kind: program|hybrid|policy；实现图与各组件 digest
├── runtime/             # 执行参数、安全链配置、恢复动作、abstain 阈值
├── verifier/            # VLM 提示词版本；C3 启用后才附加分类器 digest
├── evidence/            # 发现回合、fresh-reset 确认、验收报告、包络摘要
├── lineage.yaml         # 候选谱系、假设链、数据 digest、决策记录引用
└── rollback.yaml        # 上一 revision 与撤回条件
```

**调用语义**：`check_preconditions → invoke → observe_progress → abort/recover → verify_postconditions`；前置不满足即 **abstain**（拒绝执行，不试探）。失败可选 RoboMonkey 式多候选 + Oracle 择优重试，重试成功与一次成功分开记账。

---

## ⑤ 仿真验证、真机部署、任务执行、状态反馈

### 5.1 仿真验证——预检门（WMA 不进主线）

主线仍不建高保真数字孪生（接触 sim-to-real 误差恰在任务要害）。预检门两件套：MuJoCo G1 模型（mujoco_menagerie）运动学/自碰/工作空间检查、历史回放干跑与工件沙箱。

**MuJoCo 用法边界（明确声明）**：仅无头加载现成的 G1 机器人本体模型文件（mujoco_menagerie / 宇树官方 MJCF，下载即用），当作运动学与自碰撞计算库使用；按钮、操作台、场景**从不建模**——任务相关验证全部交给真实世界（VLM Oracle + 真机回合），与桌面/仪器的碰撞防护由工作空间盒与力/电流监控承担，不依赖仿真场景。全程无 GUI、无人工场景搭建；**本系统不引入任何需要人工搭建场景的仿真器**。

**UniFoLM-WMA 降为旁路**：不为了给 C1 预筛而去微调世界模型。技能达成、自生成回合足够、留出集证明其能排序成功/失败动作之后，再考虑作为影子预测器或合成数据引擎。在此之前它对真机候选顺序无投票权。

### 5.2 真机部署——两级执行权限（对外部蓝图的修正综合）

| 权限级 | 适用 | 规则 |
|---|---|---|
| **沙箱级** | C3 残差学习器等：动作被硬约束在既定工作空间盒与残差幅度内 | **允许在线权重更新**；金丝雀协议（新学习器前 5 回合 50% 速度）；沙箱参数本身的任何修改走门控 |
| **门控级** | 新候选类别、扩大动作空间/速度、更换 Oracle、修改安全链 | 必须冻结 artifact digest → 静态/回放/运动学预检 → 影子运行（实时观测、零写入）→ 金丝雀真机回合；证据链完整后方可常态运行 |

唯一真机写权限属于安全执行链；LLM、训练器、仿真器、WMA 一律无电机权（三平面模型）。

### 5.3 任务执行——双态执行制度

| 执行态 | 用途 | 节奏 | 特征 |
|---|---|---|---|
| **诊断态** | 程序候选逐级调试、失败鉴别实验、ACT 基线归因 | 单回合，逐回合分析 | 全仪器化记录；每回合对应一个明确假设；Orchestrator 逐回合决策 |
| **训练态** | C3 沙箱在线 RL、C2 范例批量复用采数、验收连评 | 批量自主（数十回合无人） | 人只做物理复位（初期人工合盖 3 秒，预留舵机机构接口）；批次边界触发 ⑥ |

安全链七层照旧（工作空间盒硬裁剪 → 关节限位/自碰预测 → 速度/加速度限幅 → slew rate → 力/电流监控 → 异常自动撤离 → 键盘软急停作最后冗余）。BrainCo 手按压固化为参数化原语，手指自由度不进探索空间。

### 5.4 状态反馈——回合工件（首版无事件总线）

**EpisodeArtifact**（吸收外部蓝图命名，合并 v0.2 记录规范）：

```text
EpisodeArtifact
├── 身份头：候选/配置/Oracle/标定 digest、执行态标记、假设引用
├── 复位见证：reset 后场景快照
├── 同步观测：四路相机 + 本体状态 + Oracle 独立视角
├── 逐 tick 动作血统：候选原始动作 → 适配器输出 → 安全链投影 → 关节反馈
├── 动作来源标记：candidate | recovery | scripted
├── 时序遥测：控制周期、观测新鲜度、tracking error
├── 接触代理信号：电流/力矩窗口、压深估计
├── 安全事件流：各层安全链触发记录
├── Oracle：VLM 判定 + 所用终帧 digest；（诊断态）human_witness
└── 语义结局：succeeded | failed | indeterminate | aborted
```

首版用文件系统落盘 + 一个简单的回合结束钩子（脚本调用 ④ 精炼 / ⑥ 分析）。不引入 Redis。仪表盘：干净成功率滑窗、限幅率、VLM/目击分歧率、探索覆盖。

---

## ⑥ 结果分析、经验总结、上下文优化

### 6.1 结果分析——八类失败路由（合并升级）

| 失败类别 | 判定证据 | 路由动作 |
|---|---|---|
| `perception_error` | 按钮 2D/3D 定位偏差、遮挡、目标误识 | 修标定/视角/检测器；**不动任何策略** |
| `planning_error` | 定位正确但接近/接触/撤离子目标错误 | 修几何约束与子目标序列（C1 的规划级） |
| `policy_action_error` | 候选原始动作已朝错误位姿发展 | 程序候选：修对应级；学习候选：查数据覆盖，考虑换候选类别 |
| `adapter_or_limiter_error` | 原始动作合理，适配/单位/映射/限幅投影后变错 | 修 action contract 或安全链参数；**旧回合不归咎于模型** |
| `control_tracking_error` | 投影命令正确但关节反馈持续偏离 | 修控制增益/时序/频率 |
| `contact_dynamics_error` | 到达正确接触位姿但未触发按钮/开盖 | 调按压原语参数（深度/速度/保持/手型）；重复出现 → **启用 C3 残差 RL** |
| `oracle_or_recording_error` | 动作可能成功但见证缺失/不同步/不可判定 | 修证据链；该回合**不得标成功也不得当负样本** |
| `safety_abort` | 安全链触发、异常撤离 | 独立记安全案例；配置一票否决冻结 |

**归因纪律**：`FailureDiagnosis` 必须含类别、置信度、证据引用、竞争解释；证据不足输出 `unknown` 并生成鉴别实验工单，禁止编造。逐 tick 动作血统四流对比是前五类的判据来源——这正是三种事实分离（0.2）的回报。

### 6.2 实验选择器——把少样本重构为实验选择问题

证据阶梯（便宜优先）：已有回合再分析 → 离线回放 → 实时影子运行（零写入）→ 诊断态单回合 → 训练态批量。WMA 影子预测不在首版阶梯上。

```
utility(下一实验) = 预期信息增益 + 预期成功概率 − 真机成本 − 复位成本 − 安全风险
```

首版规则式即可（失败类别 × 候选假设 → 查表），积累案例后再考虑学习化。**停滞熔断**（吸收 Zetta 洞察）：同一失败聚类连续 N 批未被当前候选类别修复 → 强制切换候选类别（如程序候选 → 残差 RL），防止在错误的抽象层无限修补。

### 6.3 经验总结与上下文优化

- **经验笔记**（批次级，结构化，向量检索）：发现、证据、假设、所采取动作、下批结果——首尾闭合可追溯；
- **成功锚点**：首次成功回合完整封存；范例库（exemplar）按初始条件指纹索引；
- **配置进化**（SHAPER 式，防震荡协议照旧）：课程起始分布（反向课程外扩）、按压原语参数域、沙箱几何、执行参数；每批次单 diff、beam top-K、git 回滚、安全违规一票否决。

---

## 节点耦合：黑板是数据契约，首版是文件目录

节点之间不直接函数调用，通过共享工件交换。首版实现 = `git` 管配置/候选 + 文件系统管回合/技能，**不部署 Redis、不建六套服务**。下表是契约，不是拓扑。

| 存储（契约名） | 内容 | 首版落地 |
|---|---|---|
| 配置库 | 课程/原语参数/沙箱/执行参数 YAML | git |
| 候选账本 | 候选契约（含假设）、验证门结果、晋级状态、谱系 | git |
| 回合库 | EpisodeArtifact 全量 | 目录 + 清单文件 |
| 范例库 | 成功轨迹 exemplar + 初始条件指纹 | 回合库的一个索引 |
| 训练缓冲 | 分段可用性标记后的样本流 | 清单过滤，C3 启动时才物化 |
| 模型注册表 | 分类器/残差/巩固 checkpoint + 血统 | 目录；P0 可为空 |
| 技能库 | 技能包 + 证据 | 目录 + git tag |
| 经验库 | 经验笔记、失败聚类账、决策记录 | markdown / YAML，git |

**一轮 walkthrough（黄色按钮，程序候选主线）**：① 读上批报告（失败聚类：`contact_dynamics` 占 78%，触达但压深不足）→ 派 Coder 出候选 c-023（假设：接触速度过快导致滑移，降速+保持）→ ④ 静态/回放/运动学预检通过 → ⑤ 诊断态 3 回合：VLM 判定 2 开 1 关，操作员目击一致 → ⑥ 归因：剩余失败为 `perception_error`（侧光下关键点偏移 4mm）→ 下批工单：换检测提示词，c-023 保持 → 连续成功后 ① 决策进入 fresh-reset 确认 → 通过 → ④ 制技能包，签发安装。

---

## 人的角色最小化（双约束专章）

| 原本需要人的环节 | v0.3 替代机制 |
|---|---|
| 遥操作示教 | C1 程序候选零示教引导（HERO L1）；C2 范例自增殖；已有历史数据仅作诊断参考 |
| 在线纠正/接管 | 先验压缩（程序标称轨迹 + 有界残差）+ 反向课程 + 安全链结构性兜底 |
| **成功/失败标注** | **VLM 看图**（"盖子开了吗"）；诊断态操作员复位时顺带目击确认 |
| **难例标记** | VLM 输出 uncertain 或与目击分歧 → 该回合 indeterminate，不进训练 |
| 课程/进度信号 | 标定感知的几何量自动计算 |
| 批次决策与授权 | Orchestrator 自动；仅初始安全包络定义与（可选）安装签发保留人的一次性参与 |
| **物理复位** | **保留给人**（合盖约 3 秒/次）；批量训练态前加装舵机复位机构（一次性投入，此后全无人） |

诚实代价标注：主线 Oracle 就是 VLM 看一张图。风险是 VLM 偶发误判与训练态被骗——前者由诊断态目击兜住（C1 回合少，可承受），后者只在启用 C3 时才成为真问题，届时再加分类器与相机隔离。不为一个可能永不发生的威胁在 P0 付费。

---

## 黄色按钮 0→1 路线（v0.3 重排）

| 阶段 | 内容 | 退出判据 |
|---|---|---|
| **P0 · 环境 API 固化**（ENPIRE 的 EN 阶段） | VLM Oracle（独立相机终帧 + 提示词冻结）；诊断态目击确认通道；G1 gym 封装 + 七层安全链 + 复位脚本；EpisodeArtifact 记录器；MuJoCo 预检门；**一次性建成后冻结为不可变 API** | 空跑 10 回合：三种事实可分别回答；VLM 与目击一致率可接受（诊断态对照，非标注界面） |
| **P1 · 程序候选主战役**（诊断态） | C1 逐级构建与调试：感知级（关键点 3D 定位精度）→ 规划级（接近轨迹）→ 执行级（跟踪）→ 接触级（按压原语参数扫描）；每回合绑定假设，八类路由修对应级；对旧 ACT 做一次三动作流归因存档 | Oracle 确认的 discovery success（首次独立验证成功） |
| **P2 · 条件启用：接触残差**（训练态，仅当 P1 卡在 contact_dynamics） | 冻结 C1 标称轨迹，沙箱内 C3 残差 RLPD 在线训练；反向课程；范例复用加速 | 接触级失败率降至可接受，回到 P1 出口 |
| **P3 · 确认与巩固** | 冻结候选，fresh reset 连续 5 次自主成功（安装门槛）；随后训练态自主连评 ≥50 回合产出稳定级证据与包络摘要；C2 范例批量采数 → C4 巩固蒸馏（可选，换取速度与抗扰动） | 安装验收通过，证据封存 |
| **P4 · 安装与复利** | 签发 `press_yellow_button@1` 技能包；回归测试脚本化；启动第二技能，度量 acquisition 成本是否下降；跨技能阶段再考虑 C5/UniFoLM-VLA 与 WMA 合成数据 | 技能可 `execute()` 稳定调用；第二技能立项 |

---

## 系统是否真的在进化：度量（采纳并扩充）

- `real_rollouts_to_first_verified_success`（核心）；
- `human_minutes_to_installed_skill`（人的分钟数只应含复位与一次性安装签发）；
- `diagnosis_resolution_rate`：归因被后续证据证实的比例；
- `oracle_vlm_human_agreement`：诊断态 VLM 与目击一致率（判定系统健康度）；
- `candidate_reuse_rate`：第二技能复用已验证模块的比例；
- `fresh_reset_confirmation_rate` 与 `regression_escape_rate`；
- 第二技能相对第一技能的全要素成本下降率——**这是"自进化"成立与否的终审指标**。

---

## 风险与开放问题

| 风险 | 应对 |
|---|---|
| VLM 偶发误判（半开、遮挡、曝光） | 诊断态目击确认兜住；uncertain 记 indeterminate 不给奖励。不为此在 P0 搭模板/分类器 |
| C3 启动后策略骗过视觉判定 | **届时**才升级：用已有 VLM 判定帧训低延迟分类器 + Oracle 相机与策略观测隔离 + 假阳率监控 |
| 程序候选在接触级反复失败且残差 RL 也停滞 | 熔断切换：SimFoundry 视频生成仿真旁路（**已开源可用**：单条场景视频→OmniGibson 物理场景，铰接管线覆盖"盖子"；"机器人数据生成/训练/评测"官方标注 Coming Soon）。WMA 合成数据同属旁路，不阻塞主线。RialTo 类需人工搭建场景的旁路已剔除 |
| 沙箱在线学习的边界蠕变 | 沙箱参数修改本身必须走门控级晋级；审计脚本每批次核验实际动作分布未逾界 |
| 复位机构成为吞吐瓶颈 | P0-P1 人工复位可承受（诊断态回合少）；P2 训练态前完成舵机机构 |
| LLM 归因/候选质量不稳 | 假设纪律 + 验证门 + 单 diff + beam + 回滚；`unknown` 合法化避免编造 |
| 2026 新文献（ENPIRE/HERO 等）无代码或未复现 | 采纳其机制与接口设计，不依赖其代码；关键机制（程序候选、Oracle、残差）全部可用成熟组件自建 |

---

## 参考文献（v0.3 合并版，标注成熟度）

**总循环范式**
1. ENPIRE: *Agentic Robot Policy Self-Improvement in the Real World*. NVIDIA GEAR, 2026. [arXiv:2606.19980](https://arxiv.org/abs/2606.19980) · [项目页](https://research.nvidia.com/labs/gear/enpire/)（真机证据强；无公开代码——采其 EN/PIRE 两阶段范式与环境 API 思想）
2. HERO: *Practice Makes Policies: Bootstrapping and Consolidating Robotic Capabilities from Zero Human Demonstrations*. 2026. [arXiv:2607.26809](https://arxiv.org/abs/2607.26809)（零人工示教三级结构 = 本蓝图 C1/C2/C4 的直接依据；未见代码）
3. SHAPER: [arXiv:2608.11350](https://arxiv.org/abs/2608.11350)；Voyager: [arXiv:2305.16291](https://arxiv.org/abs/2305.16291)（配置进化与技能库范式）

**候选与学习机制**
4. SERL / HIL-SERL: [arXiv:2401.16013](https://arxiv.org/abs/2401.16013) / [arXiv:2410.21845](https://arxiv.org/abs/2410.21845)（Apache-2.0，actor-learner 与 RLPD 内核直接可用；其人在环外壳不采用）
5. RLPD: [arXiv:2302.02948](https://arxiv.org/abs/2302.02948)（MIT 代码）
6. ResiP: [arXiv:2407.16677](https://arxiv.org/abs/2407.16677)（有界残差结构）
7. Q-Planning: [arXiv:2608.21204](https://arxiv.org/abs/2608.21204)（冻结 actor + 独立 Q 从失败学习；其人工 success bit 由本蓝图 Oracle 取代）
8. VERITAS: [arXiv:2606.18247](https://arxiv.org/abs/2606.18247)（MIT；生成-验证飞轮，验证器只排序不终审）
9. AutoSERL: [arXiv:2607.01651](https://arxiv.org/abs/2607.01651)；MiDAS: [arXiv:2608.11363](https://arxiv.org/abs/2608.11363)；Max-Q: [arXiv:2608.15088](https://arxiv.org/abs/2608.15088)（单示范/残差/选择性模仿路线，均为近期预印本，参考其机制）
10. RLDG: [arXiv:2412.09858](https://arxiv.org/abs/2412.09858)（蒸馏回通用策略）
11. 反向课程：Florensa et al. [arXiv:1707.05300](https://arxiv.org/abs/1707.05300)；RFCL (ICLR 2024)；JSRL [arXiv:2204.02372](https://arxiv.org/abs/2204.02372)

**零示教引导与判定**
12. MOKA: [arXiv:2403.03174](https://arxiv.org/abs/2403.03174)；ReKep: [arXiv:2409.01652](https://arxiv.org/abs/2409.01652)；VoxPoser: [arXiv:2307.05973](https://arxiv.org/abs/2307.05973)；Code as Policies: [arXiv:2209.07753](https://arxiv.org/abs/2209.07753)（C1 程序候选的组件库）
13. AutoEval: [arXiv:2503.24278](https://arxiv.org/abs/2503.24278)（自动评测与重置）；REFLECT: [arXiv:2306.15724](https://arxiv.org/abs/2306.15724)（分层归因）
14. RoboMonkey: [arXiv:2506.17811](https://arxiv.org/abs/2506.17811)（多候选+验证器择优）

**G1 原生资产（v0.3 新增）**
15. UniFoLM-WMA-0：宇树开源世界模型-动作框架。[GitHub](https://github.com/unitreerobotics/unifolm-world-model-action) · [项目页](https://unigen-x.github.io/unifolm-world-model-action.github.io/)（**旁路件**：不进 0→1 主线；技能达成且留出集验证通过后才考虑影子预测/合成数据）
16. UniFoLM-VLA-0：宇树开源 G1 操作 VLA（Qwen2.5-VL-7B 底座）。[GitHub](https://github.com/unitreerobotics/unifolm-vla)（动作契约与 BrainCo 26D 的适配需自建）
17. MuJoCo Menagerie G1 模型：[github.com/google-deepmind/mujoco_menagerie](https://github.com/google-deepmind/mujoco_menagerie)；LeRobot / agentlace / unitree_sdk2（工程基座）

**旁路与综述**
18. SimFoundry: *Modular and Automated Scene Generation for Policy Learning and Evaluation*. NVIDIA GEAR, 2026. [arXiv:2606.28276](https://arxiv.org/abs/2606.28276) · [项目页](https://research.nvidia.com/labs/gear/simfoundry/) · [官方开源仓库 NVlabs/SimFoundry](https://github.com/NVlabs/SimFoundry)（**Apache-2.0，2026-08-14 首发 V0**：单条真实场景视频→OmniGibson 物理场景，含刚性体与铰接物体生成、数字表亲变体，全程无需人工标注，sim-real 成功率相关性 0.911。依赖较重：NVIDIA GPU + 约 250GB 磁盘 + 门控 HF 模型 + Gemini API；"机器人数据生成/训练/评测"环节官方标注 Coming Soon。RialTo 类需人工扫描/搭建场景的方案已从本蓝图剔除）；Eureka/DrEureka: [arXiv:2406.01967](https://arxiv.org/abs/2406.01967)
19. 自进化智能体综述：[arXiv:2507.21046](https://arxiv.org/abs/2507.21046)、[arXiv:2508.07407](https://arxiv.org/abs/2508.07407)
20. 外部蓝图引用待独立复核项：PhyAgentOS、Zetta、Teach and Grow、SiLRI、LWD、π*0.6/RECAP、DGAC（本蓝图未将其列为依赖，仅作领域图景参考）

---

*文档版本 v0.3 · 2026-08-25 · 零人工标注约束落地；策略后端解绑 ACT（程序候选优先 + UniFoLM 世界模型/VLA 入列）；完成与外部蓝图的第一性原理对比吸收。取代 v0.2。*

*v0.3 原地修订（2026-08-25）：明确 MuJoCo 用法边界——仅无头加载现成 G1 本体模型做运动学/自碰预检，操作台与场景从不建模，系统不引入任何需人工搭建场景的仿真器；剔除 RialTo 旁路，替换为 SimFoundry 式"单视频→自动仿真"。*

*v0.3 原地修订三（2026-08-25）：第一性原理承认 Oracle 小题大做。新增 T6（记录做加法、机制做减法）。主线判定改为 **VLM 看图**，诊断态目击为免费审计；开合校准仪式 / 模板差分 / 轻量分类器移出 P0，仅在 C3 启动时随需构建。UniFoLM-WMA 与 Redis 事件总线移出主线。§0.6 给出全篇过度设计审计表。*
