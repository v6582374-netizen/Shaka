# 具身技能获取与验证架构蓝图

> **文档状态**：本文用于说明研究方向、系统边界与候选方法，不是首版接口规范，也不是当前实现状态报告。具有约束力的实施决策见 [ADR-0001：只冻结真机研究闭环的跨候选契约](docs/adr/0001-freeze-only-the-physical-research-loop-contracts.md) 与 [ADR-0002：通过一条纵向闭环抵达首次可信成功](docs/adr/0002-reach-first-success-through-one-vertical-slice.md)。术语以 [CONTEXT.md](CONTEXT.md) 为准；发生冲突时，以这三份文档为准。
>
> **命名说明**：文件名为保持历史链接稳定而保留。当前系统应称为“技能获取系统”，不能仅凭架构设计或一次成功称为“自进化系统”。只有第二个独立技能的获取成本按预先定义的口径显著下降后，才能称为“已验证的自进化”。

## 1. 目标与边界

目标平台是宇树 G1 人形机器人，配备机械臂、BrainCo 灵巧手和四路相机。第一阶段只解决一个任务：让机器人从允许的初始底座位姿出发，在双脚保持固定的条件下，自主定位并用指定指尖接触黄色按钮，随后结束接触并撤离设备。

黄色按钮任务的结果只由“指定指尖完成接触并撤离”决定。按钮是否产生行程、设备盖是否打开以及设备是否出现后续响应，都不属于任务结果。

首个里程碑不是建设通用机器人学习平台，也不是安装一个高可靠技能，而是取得一次可复核的首次可信成功。该成功必须满足以下条件：

1. 使用新的单次调用执行，调用中只有一次接触尝试；
2. 所有执行命令都经过唯一机器人控制入口和固定的硬件保护规则；
3. 调用证据完整，能够区分候选输出、实际执行命令、机器人反馈和保护边界介入；
4. 独立任务评估器根据记录证据判断为 `succeeded`；
5. 人只在自动结果出现后检查原始视频，确认指定指尖已经接触并撤离，未提供动作指导或逐回合训练标签。

首次可信成功出现后，当前 0→1 路线立即结束。技能安装、可靠性提升、泛化验证和第二技能复用属于后续工作，不能提前成为首版阻塞项。

## 2. 第一性原理

技能获取的本质是：根据真实物理结果，产生并保留更好的可执行行为。由此只能推导出四类必要能力：

1. **产生候选实现**：生成可执行程序、策略或混合实现；
2. **受控执行**：在固定任务契约和硬件保护边界内运行候选；
3. **独立评估**：由候选之外的机制判断证据是否满足任务契约；
4. **基于证据改进**：根据结果和失败阶段修改候选，并保留可追溯关系。

数据库、消息总线、模型注册服务、向量经验库、工作流平台、多智能体角色系统和高保真数字孪生都不是上述能力的必然前提。只有真实瓶颈证明需要它们时，才应增加相应机制。

### 2.1 三种不能混淆的事实

一次调用结束后，至少存在三种不同事实：

```text
候选正常返回
≠ 执行命令被机器人正确跟踪
≠ 任务契约得到满足
```

候选正常返回只能说明软件执行完成。机器人反馈与命令一致，只能说明控制链完成了预期动作。只有独立任务评估器确认指定指尖接触黄色按钮并撤离，才构成任务成功。

记录系统必须保留这三层证据，不能用候选的成功声明、命令发送完成或关节到位替代任务结果。

### 2.2 记录完整性优先，机制复杂度按需增加

真机证据一旦遗漏通常无法重建，因此每次调用都应完整记录。与此相反，学习机制、基础设施和抽象接口应保持最少，只在已有证据揭示具体瓶颈时增加。

这意味着：

- 可以保存较完整的观测、动作和评估证据；
- 不应为了设想中的第二技能，预先建设通用平台；
- 不应为了可能用到的强化学习，提前固定奖励模型、训练服务或模型类别；
- 不应为了减少少量真机回合，先投入更昂贵且尚未验证的仿真或世界模型系统。

## 3. 当前应建设的纵向闭环

首版只需要一条本地纵向闭环：

```text
就绪检查
→ 开始记录
→ 运行一个候选实现
→ 释放控制权
→ 完成调用证据
→ 独立自动评估
→ 根据结果复位或停止
```

调用运行器只负责生命周期编排，不包含感知、规划、控制或学习逻辑。候选实现可以更换，但以下边界保持稳定：

- 任务调用契约；
- 候选准入契约；
- 独立评估契约；
- 复位与就绪契约；
- 调用证据记录格式；
- 唯一机器人控制入口及其硬件保护规则。

首版通过版本化文件和本地命令行工具交换数据，不部署常驻服务。

### 3.1 候选准入顺序

候选被允许通过唯一机器人控制入口执行真机命令前，依次经过：

1. **静态检查**：候选包、依赖、输入输出维度、关节顺序和配置完整；
2. **历史证据离线回放**：候选能够读取既有观测，并产生格式正确、可追溯的输出；
3. **零写入端到端运行**：连接真实相机和机器人状态，运行完整链路，但禁止发布执行命令；
4. **单回合金丝雀验证**：选择当前证据支持的候选，以低速执行一次单次尝试。

前三个阶段失败不消耗真机回合。金丝雀出现接入或部署缺陷时，退回零写入阶段；出现候选行为失败时，保留证据并形成新的改进假设。

### 3.2 复位与停止

当结果为 `failed` 或 `indeterminate` 时，候选之外的固定程序通过唯一机器人控制入口恢复标准机械臂和灵巧手起始状态。只有机器人状态、相机、记录器和控制权检查全部通过，才能创建下一次调用。

当结果为 `aborted` 时，系统停止自主循环，等待人工检查，不能自动复位后继续尝试。

以下任一情况都应停止整轮自主研究：

- 总真机回合预算或机器人运行时间预算耗尽；
- 复位失败；
- 调用证据不完整；
- 独立评估器失效；
- 出现竞争命令发布者；
- 候选绕过唯一机器人控制入口；
- 冻结的跨候选契约在研究过程中被修改。

## 4. 编程智能体的职责与自由度

首版不需要预设“指挥、编码、分析、审计”等多个智能体角色，也不需要为这些角色维护一组长期并行的提示词。它们只是同一个编程智能体在不同阶段承担的工作：提出假设、修改候选、分析证据和检查结果。

编程智能体可以自主选择、组合或创造方法，包括但不限于 UniFoLM、行为克隆、强化学习、启发式程序、代码策略和混合实现。系统限制的不是方法名称，而是方法必须遵守的证据与执行边界。

每项进入真机研究的假设至少应说明：

- 它基于哪一项已观察证据；
- 准备修改什么；
- 预期改善哪个可观察结果；
- 分配多少子预算；
- 使用什么诊断指标判断是否改善；
- 失败后回退到哪个候选。

在可以隔离变量时，每次只修改一个主要因素。这里的目的不是机械追求“单一改动”，而是让结果能够支持或反驳当前假设。

智能体的选择权受以下边界约束：

- 不能修改任务成功定义来迎合候选；
- 不能访问、替换或改写独立任务评估器；
- 不能绕过候选准入流程和唯一机器人控制入口；
- 不能把人工逐回合判断作为训练反馈；
- 不能超过人预先设置的总回合预算和机器人运行时间预算；
- 不能在硬件保护中止后自行恢复研究。

因此，合理的架构不是用封闭方法清单替代智能体决策，而是允许方法开放，同时冻结证据、权限、安全和预算边界。

## 5. 候选生成与验证循环

旧版使用“候选工厂”一词，容易让人误以为系统存在一条固定流水线，能够批量制造同构候选。更准确的描述是“候选生成与验证循环”：智能体根据证据提出一个可执行实现，通过统一准入和评估边界验证，再决定保留、修改或回退。

一个候选实现至少需要绑定：

```json
{
  "candidate_id": "candidate-v023",
  "parent_candidate_id": "candidate-v019",
  "implementation_kind": "program | policy | hybrid",
  "observed_evidence": "调用证据引用",
  "hypothesis": "降低接触速度可能减少指尖滑移",
  "expected_observation": "接触位置不变，滑移距离下降",
  "observation_contract": "候选可读取的输入",
  "action_contract": "候选允许输出的动作",
  "artifact_digest": "sha256:...",
  "sub_budget": "允许消耗的回合数或机器人时间",
  "rollback_candidate_id": "candidate-v019"
}
```

该结构表达的是候选之间的谱系和证据关系，不规定候选内部必须采用哪种模型。

### 5.1 旧版 C1–C5 的准确含义

旧版把 C1–C5 放在同一张表中，造成了两个误解：它们像是五个并列选项，并且系统似乎只能从中选择。实际上，这些条目混合了可执行实现、数据复用机制、学习组件和技能生命周期阶段，不能视为同一层级的五选一方案。

| 旧标签 | 更准确的定位 | 是否能独立成为候选 | 典型使用时机 |
|---|---|---:|---|
| C1 程序候选 | 启发式或代码策略实现 | 可以 | 直接产生一个可执行基线，或作为混合候选的标称轨迹 |
| C2 范例复用 | 成功轨迹的复用机制和数据来源 | 通常不能 | 已经出现成功轨迹后，用于重放、变换或生成后续训练数据 |
| C3 有界残差强化学习 | 附加在标称轨迹上的学习组件 | 通常作为混合候选的一部分 | 证据表明主要误差集中在可约束的小范围动作残差时 |
| C4 巩固策略 | 将已有成功数据蒸馏为闭环策略的后续优化阶段 | 可以产出新候选，但不是 0→1 的前置条件 | 已经取得足够成功数据，需要提升速度或抗扰动能力时 |
| C5 UniFoLM-VLA 适配 | 依赖特定视觉—语言—动作模型的实现方式 | 可以 | 当前证据支持其输入、动作输出和泛化能力适合任务时 |

因此，C1–C5 不是权威分类，也不是封闭白名单。新版不再用这些编号驱动实施决策，只把它们保留为历史映射。

### 5.2 方法可以组合，但不做穷举排列

候选内部可以组合多种机制。例如：

- 视觉模型定位按钮，几何规划生成接近轨迹，参数化动作完成接触和撤离；
- 行为克隆策略提供标称动作，强化学习只学习受限残差；
- UniFoLM-VLA 负责高层目标或动作建议，确定性适配器完成关节映射，硬件保护边界限制最终命令；
- 成功轨迹既可直接复用，也可用于训练新的闭环策略。

但系统不应预先枚举所有排列组合。组合数量会随组件增加而快速膨胀，而且多数组合没有证据支持。智能体应根据已观察失败选择最小必要组合，并把组合后的整体作为一个候选重新经过准入和独立评估。

### 5.3 初始方法目录

下表只是帮助智能体形成第一批假设，不代表固定顺序或优先级。

| 方法族 | 可以解决的问题 | 启用所需证据 | 主要代价 |
|---|---|---|---|
| 启发式或代码策略 | 将感知、几何规划、接触动作和撤离过程显式组织起来 | 任务可分解，关键状态可观测 | 需要维护标定和显式规则 |
| 行为克隆 | 从既有轨迹学习动作映射 | 存在质量足够且与当前任务相符的数据 | 容易继承数据分布和动作偏差 |
| 真实世界强化学习 | 通过真机结果优化策略 | 训练代理奖励已经验证并冻结，复位和批量执行可靠 | 真机样本成本高，奖励设计和执行稳定性要求高 |
| 有界残差学习 | 在已有标称轨迹附近修正小范围误差 | 失败可稳定归因于局部控制或接触偏差 | 需要证明残差边界足以覆盖问题 |
| 视觉—语言—动作模型适配 | 利用预训练模型的视觉语义和动作先验 | 模型输入、动作空间、时序和硬件适配已经验证 | 推理开销、动作映射和分布外行为难以归因 |
| 混合实现 | 让不同机制分别承担其擅长的子问题 | 组合能减少已观察瓶颈，且接口不会掩盖失败来源 | 组件越多，归因和版本管理越复杂 |

程序化实现可以是第一项低成本假设，但不是强制主路线。若现有 UniFoLM、行为克隆或其他候选已经有更直接的证据支持，智能体可以先验证它们。

## 6. 评估、训练信号与证据

### 6.1 独立任务评估器

独立任务评估器只读取完整调用证据，输出：

- 结构化视觉事实；
- `succeeded | failed | indeterminate | aborted | abstained` 之一；
- 评估器版本；
- 输入证据摘要。

候选不能访问或改写评估器。候选报告的“完成”或“成功”可以作为诊断信息保存，但不能进入任务结果字段。

### 6.2 训练代理奖励

训练代理奖励与独立任务评估器不是同一个概念。训练代理奖励用于候选比较或策略学习，不能单独证明任务成功。

只有经过一次性开发、隔离验收并冻结版本后，训练代理奖励才能用于自主研究。人的逐回合观察不能作为在线训练标签；人工记录只能独立保存为审计证据。

只有 `succeeded` 和 `failed` 可以提供任务结果标签。`indeterminate` 表示证据不足，不能作为正样本或负样本；`aborted` 表示硬件保护或执行边界中止；`abstained` 表示前置条件不满足，候选没有执行。

### 6.3 调用证据记录

每条调用证据只对应一次单次尝试，至少应包含：

- 候选标识、候选工件摘要和父候选；
- 任务意图、标准机械臂起始状态和机器人底座位姿；
- 候选实际读取的观测及其时间信息；
- 候选原始输出；
- 适配和硬件保护处理后的执行命令；
- 机器人关节、视觉和接触相关反馈；
- 硬件保护介入、控制权释放和异常信息；
- 独立评估器使用的原始证据、版本和结果；
- 与该调用关联的复位结果。下一次调用的就绪检查单独记入下一条调用记录。

首版将这些内容保存在文件系统中，并通过清单文件建立引用关系。只有真实规模或并发瓶颈出现后，才考虑数据库、消息总线或专用工件服务。

## 7. 失败归因与下一项实验

失败归因必须从直接证据出发。诊断结论是对证据的解释，不能替代或改写原始记录。证据不足时应输出“不确定”，并设计能够区分竞争解释的下一项实验。

以下是可扩展的初始诊断维度，不是冻结分类：

| 观察到的阶段 | 需要检查的证据 | 可能的下一步 |
|---|---|---|
| 任务理解或目标定位 | 目标选择、二维或三维位置、遮挡和观测时序 | 修正观测、标定或目标定位方法 |
| 接近与撤离规划 | 子目标、轨迹和工作空间约束 | 修改几何约束或路径生成 |
| 候选动作生成 | 候选原始输出是否已经偏离目标 | 修改程序、策略、数据或模型 |
| 动作适配与限制 | 单位、关节映射、限幅前后命令 | 修正适配器或保护参数，并重新过准入门 |
| 控制跟踪 | 执行命令与关节反馈的时间对齐和误差 | 检查控制频率、延迟和控制器参数 |
| 接触与撤离 | 指尖位置、接触持续时间、滑移和撤离结果 | 调整动作参数，或验证是否需要局部学习 |
| 记录与评估 | 证据是否完整、同步，评估输入是否充分 | 修复证据链；该调用不作为训练样本 |
| 硬件保护中止 | 保护规则、原始命令和中止时刻 | 停止自主循环并进行人工检查 |

选择下一项实验时，优先使用成本较低的证据：

```text
重新分析已有证据
→ 历史证据离线回放
→ 真实观测零写入运行
→ 单回合真机实验
```

只有需要策略学习且复位、评估和安全边界已经可靠时，才进入预算内的批量真机研究。

## 8. 人的角色

人负责定义任务、总预算和硬件保护边界，并在硬件保护中止后检查现场。首次自动成功出现后，人检查原始视频，独立确认任务契约是否满足。

人不承担以下工作：

- 遥操作示教；
- 在线动作纠正或接管；
- 逐回合提供训练标签；
- 为每个候选选择具体算法；
- 在自主研究过程中修改任务成功定义。

首版若仍需要人工完成物理环境复位，应将其明确计入人工时间和实验吞吐成本，不能把“无需训练标注”表述为“无需人工参与”。

## 9. 从首次可信成功到已安装技能

首次可信成功证明纵向技能获取闭环已经成立，但它不是已安装技能。

后续安装工作至少需要另行决定：

- 独立复现次数和初始条件覆盖；
- 回归检查范围；
- 版本、调用接口和回滚规则；
- 允许的机器人底座位姿范围；
- 失败、拒绝执行和保护中止的服务语义；
- 安装后监测和撤回条件。

这些要求应由首次可信成功产生的真实证据驱动，不能在 0→1 之前用假定数字冻结。旧版中的“连续 5 次成功”“至少 50 回合验证”等数值因此不再作为当前架构要求。

## 10. 如何验证系统是否发生自进化

一次技能获取成功只能证明闭环有效，不能证明系统发生了自进化。

在开始第二个独立技能前，应先冻结成本口径，例如：

- 首次可信成功所需真机回合数；
- 机器人运行时间；
- 人工分钟数；
- 候选开发和计算成本；
- 因接入、记录或评估缺陷浪费的回合数。

只有第二个独立技能在预先声明的同类指标上出现实质性下降，并且下降能够归因于第一个技能留下的可复用资产或改进后的研究流程，才能称为“已验证的自进化”。模型版本增加、候选数量增加或代码量增加都不是自进化证据。

## 11. 后续研究机制

以下机制可以保留在研究目录中，但不进入首版承诺：

- 使用成功轨迹进行范例复用或闭环策略蒸馏；
- 在动作空间受到严格约束时进行残差强化学习；
- 将 UniFoLM-VLA 或其他视觉—语言—动作模型作为候选实现；
- 在已有真机数据足以进行独立验证后，评估世界模型的影子预测或合成数据价值；
- 在第二个独立技能暴露真实重复后，提取跨技能接口、技能库或模型管理能力；
- 在实验并发和数据规模证明文件系统不足后，引入数据库或消息基础设施。

每项机制都必须由当前证据说明它解决了什么瓶颈。仅仅“将来可能有用”不能构成建设理由。

## 12. 主要风险与处理原则

| 风险 | 处理原则 |
|---|---|
| 自动评估误判接触或撤离 | 保留原始视频和结构化视觉事实；结果不确定时输出 `indeterminate`，不强行二分类 |
| 学习策略利用训练代理奖励漏洞 | 将训练代理奖励与独立任务评估器分离；冻结奖励版本；发现分布漂移时暂停训练 |
| 候选通过限幅或保护投影后偶然成功 | 同时检查候选原始输出、执行命令和硬件保护介入，避免把不可复现的“脏成功”当作能力 |
| 复位成为真机吞吐瓶颈 | 先测量实际人工时间；只有吞吐证据支持时才建设自动复位机构 |
| 智能体反复修改错误层级 | 要求假设引用证据，并使用能够区分竞争解释的实验，而不是连续盲目调参 |
| 新研究方法尚未复现 | 把论文作为假设来源，不把论文结论直接当作当前系统能力 |

## 13. 研究参考

下列资料用于提供候选方法和研究机制，不构成首版依赖。论文成熟度、开源状态和与 G1、BrainCo 灵巧手的适配成本，应在实际采用前单独核验。

### 技能获取与策略改进循环

1. ENPIRE: *Agentic Robot Policy Self-Improvement in the Real World*. NVIDIA GEAR, 2026. [arXiv](https://arxiv.org/abs/2606.19980) · [项目页](https://research.nvidia.com/labs/gear/enpire/)
2. HERO: *Practice Makes Policies: Bootstrapping and Consolidating Robotic Capabilities from Zero Human Demonstrations*. 2026. [arXiv](https://arxiv.org/abs/2607.26809)
3. SHAPER. [arXiv](https://arxiv.org/abs/2608.11350)
4. Voyager. [arXiv](https://arxiv.org/abs/2305.16291)

### 强化学习、残差学习与策略蒸馏

5. SERL. [arXiv](https://arxiv.org/abs/2401.16013)
6. HIL-SERL. [arXiv](https://arxiv.org/abs/2410.21845)
7. RLPD. [arXiv](https://arxiv.org/abs/2302.02948)
8. ResiP. [arXiv](https://arxiv.org/abs/2407.16677)
9. Q-Planning. [arXiv](https://arxiv.org/abs/2608.21204)
10. RLDG. [arXiv](https://arxiv.org/abs/2412.09858)
11. Reverse Curriculum Generation. [arXiv](https://arxiv.org/abs/1707.05300)

### 程序化操作、自动评估与失败分析

12. MOKA. [arXiv](https://arxiv.org/abs/2403.03174)
13. ReKep. [arXiv](https://arxiv.org/abs/2409.01652)
14. VoxPoser. [arXiv](https://arxiv.org/abs/2307.05973)
15. Code as Policies. [arXiv](https://arxiv.org/abs/2209.07753)
16. AutoEval. [arXiv](https://arxiv.org/abs/2503.24278)
17. REFLECT. [arXiv](https://arxiv.org/abs/2306.15724)
18. RoboMonkey. [arXiv](https://arxiv.org/abs/2506.17811)

### G1 相关资产与仿真研究

19. UniFoLM-WMA-0. [GitHub](https://github.com/unitreerobotics/unifolm-world-model-action) · [项目页](https://unigen-x.github.io/unifolm-world-model-action.github.io/)
20. UniFoLM-VLA-0. [GitHub](https://github.com/unitreerobotics/unifolm-vla)
21. MuJoCo Menagerie G1 模型. [GitHub](https://github.com/google-deepmind/mujoco_menagerie)
22. SimFoundry. [arXiv](https://arxiv.org/abs/2606.28276) · [项目页](https://research.nvidia.com/labs/gear/simfoundry/) · [GitHub](https://github.com/NVlabs/SimFoundry)

---

*文档版本：v0.4 · 2026-08-27。此次修订以现行领域术语和 ADR 为准，移除了多智能体角色系统、封闭候选分类、设备盖状态判定和预设安装门槛等过时描述；将“候选工厂”改写为开放的候选生成与验证循环，并明确了智能体的方法选择权。*
